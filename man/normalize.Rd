% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{normalize}
\alias{normalize}
\title{Normalize Expression Data in SCE/SPE Objects}
\usage{
normalize(object, method = "cpm", assay = "counts")
}
\arguments{
\item{object}{A \code{SingleCellExperiment} or \code{SpatialExperiment} with raw counts.}

\item{method}{Normalization method: \code{"cpm"}, \code{"logcpm"}, or \code{"logpf"}.}

\item{assay}{Assay to normalize (default: "counts").}
}
\description{
Adds a normalized assay (\code{cpm}, \code{logcpm}, or \code{logpf}) to a
\code{SingleCellExperiment} or \code{SpatialExperiment}. The original assay
is left unchanged. \code{cpm} computes counts per million, \code{logcpm} is
\code{log(cpm + 1)}, and \code{logpf} is \code{log((counts + 1) / (gene_mean + 1))}
with gene-wise means computed from \code{counts + 1}.
}
